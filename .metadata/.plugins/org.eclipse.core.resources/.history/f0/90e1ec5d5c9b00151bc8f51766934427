package spring.hashcul.dao;

import java.util.ArrayList;

import org.mybatis.spring.support.SqlSessionDaoSupport;

import spring.hashcul.model.ContactVo;
import spring.hashcul.model.ListVo;

public class ListDaoImpl extends SqlSessionDaoSupport implements ListDao {

	public ArrayList<ListVo> get(String[] categories, String[] locations, String startDate, String endDate) {
		
		ListVo list = (ListVo)getSqlSession().selectOne("contactdao.getList", categories, locations, startDate, endDate);
		
		
		//return list;
	}
	
	

}
